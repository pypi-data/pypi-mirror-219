[project]
name = "smart_notes_robot"
version = "0.1.0"
description = ""
authors = [
    { name = "Jan Kolomaznik", email = "jan.kolomaznik@gmail.com" },
]
dependencies = [
    "pyyaml>=6.0",
]
requires-python = ">=3.10"
readme = "readme.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.dev-dependencies]
test = [
    "pytest>=7.3.2",
]
dev = [
    "jupyter>=1.0.0",
]

[tool.pdm.scripts]
tests = "pytest --junitxml=report.xml"
