[tool.poetry]
name = "swarkn"
version = "0.1.0"
description = ""
authors = ["John Smith"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
