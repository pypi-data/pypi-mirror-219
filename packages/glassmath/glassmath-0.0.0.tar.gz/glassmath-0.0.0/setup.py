from setuptools import setup, find_packages

setup(
    name='glassmath',
    author='PyModuleDev',
    description='[IN BETA TESTING]More advanced version of the math module. Protected by Apache 2.0 License.',
    packages=find_packages(),
    install_requires=[
        # List any dependencies required by your module
    ],
)
