[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "giadep"
version = "0.0.2"
authors = [
  { name="Infstellar" },
]
description = "Dependence for GIA."
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
    ]

[tool.hatch.build]
include = [
  "*"
]