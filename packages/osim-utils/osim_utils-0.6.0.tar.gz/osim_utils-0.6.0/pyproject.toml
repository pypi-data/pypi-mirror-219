[tool.poetry]
name = "osim-utils"
version = "0.6.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "osim_utils"}]

[tool.poetry.dependencies]
python = "^3.10"
python-json-logger = "^2.0.4"
requests = "^2.28.2"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
