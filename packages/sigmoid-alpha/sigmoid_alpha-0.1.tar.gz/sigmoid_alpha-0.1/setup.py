from setuptools import setup, find_packages

setup(name = 'sigmoid_alpha',
      version = '0.1',
      author = 'sigmoid_capital',
      author_email = 'sigmoid_capital@outlook.com',
      description = 'find alpha signal',
      packages = find_packages(),
      install_requires = [])
