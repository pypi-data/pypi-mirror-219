from setuptools import setup, find_packages

setup(
    name="LjxMathTest",
    version="1.0.1",
    author="crazydone",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here (if any)
    ],
)
