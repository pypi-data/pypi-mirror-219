from distutils.core import setup
import setuptools

packages = ['pbix_plus']
setup(name='pbix_plus',
      version='23.7.3',
      author='xg',
      packages=packages,
      package_dir={'requests': 'requests'}, )
