[tool.poetry]
name = "fastapi-yaml"
version = "0.1.1"
description = ""
authors = ["Mohamed Cherkaoui <chermed@gmail.com>"]
readme = "README.md"
packages = [{include = "fastapi_yaml"}]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.100.0"
pyyaml = "^6.0"


[tool.poetry.group.dev.dependencies]
ipdb = "^0.13.13"
pre-commit = "^3.3.3"
httpx = "^0.24.1"
pytest-cov = "^4.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
