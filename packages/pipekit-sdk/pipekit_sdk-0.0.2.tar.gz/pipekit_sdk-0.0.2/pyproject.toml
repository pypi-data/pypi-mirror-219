[build-system]
requires = ["hatchling", "hera >= 5.5.2"]
build-backend = "hatchling.build"

[project]
name = "pipekit-sdk"
version = "0.0.2"
authors = [
  { name="Aleksa Trajkovic", email="aleksa@pipekit.io" },
]
description = "Pipekit Python SDK"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

dependencies = [
  "hera >= 5.5.2",
]
