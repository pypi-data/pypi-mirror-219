[tool.poetry]
name = "envutil"
version = "0.2.3"
description = ""
authors = ["Berke Arslan <berke@kwilabs.com>"]
readme = "README.md"
packages = [
    { include = "envutil", from = "." },
    { include = "py.typed", from = "." }
]

[tool.poetry.dependencies]
python = "^3.11"
python-dotenv = "^1.0.0"


[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
isort = "^5.12.0"
flake8 = "^6.0.0"

[tool.isort]
profile = "black"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
